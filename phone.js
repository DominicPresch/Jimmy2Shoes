Pnumber(num){
	if(isnum(num)){
		if(num.length = 10){
			return num;}
		else if(num.length = 5
			return num;
		else if(num = '911')
			return num;
		else
			return 'Phone number may be incorrect';
	}else{
		return 'That is not a valid Phone number';
	}

}

	var isnum(x){
		if(x= /^\d+$)
			return true;
		else
			return false;
	}
